apiVersion: v1
kind: Service
metadata:
  name: {{ include "orpheus.fullname" . }}
  labels:
    {{- include "orpheus.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "orpheus.selectorLabels" . | nindent 4 }}