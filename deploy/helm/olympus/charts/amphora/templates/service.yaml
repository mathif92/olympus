apiVersion: v1
kind: Service
metadata:
  name: {{ include "amphora.fullname" . }}
  labels:
    {{- include "amphora.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "amphora.selectorLabels" . | nindent 4 }}