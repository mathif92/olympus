{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "hephaestus.saName" . }}
  labels:
    {{- include "hephaestus.labels" . | nindent 4 }}
{{- end }}
