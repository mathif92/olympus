apiVersion: v1
kind: Service
metadata:
  name: {{ include "hephaestus.fullname" . }}
  labels:
    {{- include "hephaestus.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "hephaestus.selectorLabels" . | nindent 4 }}