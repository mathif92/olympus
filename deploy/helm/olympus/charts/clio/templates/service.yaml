apiVersion: v1
kind: Service
metadata:
  name: {{ include "clio.fullname" . }}
  labels:
    {{- include "clio.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "clio.selectorLabels" . | nindent 4 }}