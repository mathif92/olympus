{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "prometheus.saName" . }}
  labels:
    {{- include "prometheus.labels" . | nindent 4 }}
{{- end }}
