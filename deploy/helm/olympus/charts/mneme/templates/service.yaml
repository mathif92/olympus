apiVersion: v1
kind: Service
metadata:
  name: {{ include "mneme.fullname" . }}
  labels:
    {{- include "mneme.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "mneme.selectorLabels" . | nindent 4 }}